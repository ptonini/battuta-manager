---
- name: Deploy Worker
  hosts: m2m-worker
  become: yes
  vars:
    mode: deploy
      
  roles:
    - role: java 
      when: mode == 'install'
      
    - role: m2m-worker
    
    - role: m2m-watchdog
      config: 'service'
      when:  mode == 'install'
